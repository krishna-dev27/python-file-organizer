# 📁 Python File Organizer

A simple Python automation script that organizes files in a folder based on file type.

## Features
- Sorts files into Images, Documents, Music, Videos, and Others
- Works for any folder path (user input)
- Automatically creates folders if not present
- Uses only built-in Python modules

## How to Run
1. Install Python 
2. Run the script:
   ```bash
   python file_organizer.py
